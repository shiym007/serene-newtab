# 更新日志

## v1.6.1 — 2026-08-19

### 新增功能

**移动端 / 触屏适配**
- 编辑布局新增「点选 + ＋/－ 缩放」：触屏设备没有滚轮，点击任意元素选中后，用浮动按钮放大 / 缩小，实时显示当前倍率
- 触屏设备上悬停才出现的删除按钮（联想条目、吸附线、快捷方式）改为常显，并放大命中区至 ≥26px
- 刘海屏 / 手势条安全区适配：顶栏、完成按钮、缩放控件、设置面板自动避让 `env(safe-area-inset-*)`

### 功能改进
- 视口配置升级：`viewport-fit=cover` 配合安全区，全面屏不留黑边
- 禁用下拉刷新与橡皮筋（`overscroll-behavior: none`），去除点击高亮与双击缩放延迟（`touch-action: manipulation`）
- 窄屏断点（≤640px）：搜索框加高至 52px、输入字号 ≥16px 防 iOS 聚焦自动放大、图标与面板尺寸自适应；≤400px 隐藏快捷方式名称
- 联想列表支持触摸纵向滑动（`touch-action: pan-y`），高度限制在 42vh 内
- 编辑模式提示按设备自动切换文案（触屏提示点选缩放，桌面提示滚轮缩放）

---

## v1.6.0 — 2026-08-18

### 新增功能

**布局系统**
- 支持自定义吸附线：可在设置中添加任意条辅助线，拖动线身移动位置、拖动端点旋转任意角度（0°/90°/180°/270° 自动磁吸）
- 支持元素吸附到吸附线：拖动时钟 / 日期 / 搜索框 / 快捷方式靠近吸附线时自动吸附到线上，沿线自由排列，优先于居中吸附
- 支持吸附线编辑模式下的高亮反馈与悬停删除，扩大线身与手柄的可点击范围

**壁纸调节**
- 支持壁纸亮度调节（40%–120%），对渐变 / 图片 / 视频背景统一生效
- 支持壁纸常驻模糊（0–30px），与聚焦模糊独立叠加

**日期系统**
- 支持日期作为独立自由块：位置、大小可单独拖拽缩放，与时钟完全解耦
- 支持日期格式自定义引擎：`{Y} {y} {M} {m} {ME} {D} {d} {W} {w} {WE}` 十个占位符任意组合，实时预览
- 支持日期字体三选（衬线 / 黑体 / 等宽）
- 支持时钟加粗、日期加粗独立开关

**搜索增强**
- 支持搜索框仅图标模式：平时收起为圆形磨砂图标，点击弹性展开，失焦自动收起
- 支持搜索框宽度调节（360–900px），联想栏宽度自动跟随
- 支持联想条目数设置（5–12 条）
- 支持搜索栏透明度、快捷方式图标透明度独立调节
- 支持搜索 / 快捷方式「在新标签页打开」两个独立开关（关闭则当前页加载）

**动画**
- 支持聚焦搜索时快捷方式以搜索栏为中心向外扩散消失（径向退场模式），总时长钳制在背景模糊完成前
- 支持打开标签页的完整入场编排：壁纸淡入 → 主体元素依次浮现 → 顶栏下滑 → 图标错峰弹入

**其他**
- 支持关闭时钟、关闭左上角 SERENE 字样、关闭快捷方式名称显示

### 功能改进
- 重构设置面板为七个逻辑分区：界面 / 时间 / 日期 / 背景 / 布局 / 快捷方式 / 搜索
- 编辑布局入口由开关改为按钮，进入/退出文案随状态切换
- 时钟数字切换改为逐位 diff 过渡动画，字体样式切换增加淡入淡出
- 快捷方式图标获取增加 `.png` / `.svg` 候选回退（修复部分 SPA 站点图标无法识别）
- 修复搜索后当前标签页同时跳转的问题（仅新标签页跳转）
- 性能重构：拖动 rAF 合帧 + 单元素精准写入、列表渲染全事件委托、日期串按天缓存、DocumentFragment 批量插入

### 升级说明
- 旧版设置的日期样式将自动迁移为对应的自定义格式串，无需手动处理

---

## v1.5.0 — 2026-08-17

### 新增功能
- 支持对接 Bing 搜索记录：内容脚本捕获地址栏搜索、Bing 页内搜索（含图片搜索），与新标签页搜索历史合并
- 支持搜索历史逐条删除：联想条目悬停「×」即删，设置面板内提供历史管理列表

### 功能改进
- 时钟冒号改为缩小并垂直居中，静止不闪

---

## v1.4.0 — 2026-08-17

### 新增功能
- 支持时钟数字变化过渡动画与字体样式切换过渡
- 支持快捷方式名称显示开关
- 支持四款日期样式：点阵 / 汉字 / 斜杠 / 英文

### 问题修复
- 修复快捷方式图标磨砂玻璃效果丢失的问题（will-change 导致 backdrop root）
- 提升日期文字在亮色壁纸上的可读性

---

## v1.3.0 — 2026-08-16

### 新增功能
- 支持快捷方式每个网站独立图标：位置、大小单独调整，互不影响
- 支持快捷方式显隐动画重构：退场干脆、回归带弹簧曲线错峰弹出
- 支持搜索与快捷方式在新标签页打开
- 支持时钟冒号缩小底置（v1.6 改为居中）

### 功能改进
- 衬线时钟字体更换为等高 lining 数字，修复高度参差不齐
- 移除页面「+」添加按钮，快捷方式管理统一收入设置面板

---

## v1.2.0 — 2026-08-16

### 新增功能
- 支持时钟四款字体样式（衬线 / 纤细 / 等宽 / 简黑）
- 支持联想框自适应屏幕：不越界、与边缘保持安全距离、空间不足自动向上展开
- 支持布局编辑模式：拖动 / 缩放 / 居中磁性吸附
- 支持快捷方式系统：自定义图标、favicon 回退、首字母底块

### 功能改进
- 修复外部字体 CDN 挂起导致新标签页空白的问题，全面改用本地字体栈

---

## v1.0.0 — 2026-08-16

- 首个版本：大字时钟、Bing 搜索联想、聚焦高斯模糊、六款渐变背景、自定义图片背景、基础设置
